# imageread

## DESCRIPTION
可以将图片发送给模型，让模型根据图片内容理解返回内容，该功能最好配合agenttool进行。
## PROMPT
当用户需要理解、描述、总结或判断图片内容时使用本工具。

输入 image_url 必须是可访问的图片 URL。
工具内部会下载图片并转为 base64 发送给视觉模型，但不会在日志中保存 base64 内容。

不要用本工具处理非图片 URL。
如果图片 URL 失效，应明确说明无法下载图片。